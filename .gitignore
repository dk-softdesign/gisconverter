node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Local sample/test files (real customer data, not part of the app)
/source_files/

# Personal Claude Code overrides (not shared with the team)
.claude/settings.local.json
